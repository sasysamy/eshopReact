# eshopReact
To use react for working on the sample task.


The above task is completed using React, ES6 and Webpack.

To install all the dependencies just enter the command `npm install` and to run the server use `npm run server`.

The application will open in [http://localhost:3000](http://localhost:3000/)

